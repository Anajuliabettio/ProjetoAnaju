document.getElementById('Formulariodecontato').addEventListener('submit', function(event) {
    event.preventDefault();
    alert('Mensagem enviada com sucesso!');
    // Aqui você vai adicionar código para enviar o formulário via wpp, depois
});